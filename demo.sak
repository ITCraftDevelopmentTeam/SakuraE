import std.io;
import std.str;
import std.err;

func main(args: String) -> void {
    try {
        let a = input("plz, input: ");

        for (let ch = range a) {
            println(ch);
        }
    }
    catch (let e: Error) {
        e.printDetails();
    }
}

func fib(n: int) -> int {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

func main() -> int {
    let str = "Hello world";
    let arr = [1,2,3,4];
    arr[0] = 114514;
    str[0] = 'F';
    __println(str);
    return fib(21) + arr[0];
}

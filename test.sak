func main(a: int, b: int) -> int {
    let g = [1,2,3];
    for (let i=0; i<a+b; i++) {
        if (i <= 2) {
            g[i] += i*2;
        }
        else {
            g[0] += i*2
        }
    }
    return g[0];
}